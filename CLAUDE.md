# プロジェクト指示

## 自動ビルドルール

**重要:** このプロジェクトのコードを変更した場合、**必ずビルドを実行してください**。

### 手順

1. コードを変更する
2. **即座にビルドを実行**: `cd /Users/yoshi/dev/Arduino/gardia_radar && ./gradlew assembleDebug`
3. ビルド結果をユーザーに報告

### 例

**ユーザー:** 「時刻同期のタイムアウト時間を変更して」

**Claudeの応答:**
```
✅ 変更完了しました！

ビルドを実行します...
[ビルド結果を表示]
```

## ビルドコマンド

```bash
cd /Users/yoshi/dev/Arduino/gardia_radar && ./gradlew assembleDebug
```

## ⚠️ 禁止事項

### インストールについて
**ユーザーの明示的な許可なく、アプリをインストールしないでください。**

ビルド完了後、APKが生成されたことを報告するのみで、`adb install`などのインストールコマンドを実行してはいけません。

### ログ監視について
**ユーザーの明示的な許可なく、ログ監視（Monitor、adb logcatなど）を実行しないでください。**

バックグラウンドでのログ監視は問題を引き起こす可能性があるため、ユーザーの許可なしに実行してはいけません。

## プラットフォーム情報

- **種類**: android
